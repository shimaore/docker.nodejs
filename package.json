{
  "name": "docker.nodejs"
, "version": "0.10.36"
, "private": true
, "scripts": {
    "install": "make"
  , "pretest": "npm install"
  , "test": "cd test && for test in ./*.sh; do \"$test\"; done"
  }
}
